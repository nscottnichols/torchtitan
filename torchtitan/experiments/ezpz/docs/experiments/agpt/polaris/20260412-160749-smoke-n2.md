# agpt Smoke Test -- Polaris 2-node (2026-04-12)

## Environment

| Field | Value |
|-------|-------|
| Date | 2026-04-12 |
| Commit | `9aa2ba89` |
| Machine | Polaris |
| Job ID | 7088135, 7088216 |
| Nodes | 2 |
| Devices | 8x NVIDIA A100-SXM4-40GB |
| Steps | 50 |
| Dataset | books corpus |
| Backend | NCCL |
| Compile | enabled (default) |

## Model Configurations

| Config | Total Params | Layers | Heads | Dim | LBS | seq_len |
|--------|-------------|--------|-------|-----|-----|---------|
| debugmodel | ~4.5M | 6 | 16 | 256 | 2 | 8192 |
| 2B | ~2B | 12 | 16 | 2048 | 2 | 8192 |
| 20B | ~20B | 64 | 40 | 5120 | 1 | 4096 |
| 80B | ~80.8B | 84 | 72 | 9216 | 1 | 4096 |

## Results

| Config | Final Loss | Memory | TPS | TFLOPS | MFU | Wall Time | AC Mode | Parallelism | W&B | Status |
|--------|-----------|--------|-----|--------|-----|-----------|---------|-------------|-----|--------|
| debugmodel | 6.82 | 2.23 GiB (5.66%) | 108,980 | 25.16 | 8.06% | 242.55s | full | FSDP=8 | [proud-bush-1071](https://wandb.ai/aurora_gpt/torchtitan.ezpz.train/runs/z8zacrf3) | PASS |
| 2B | 7.28 | 24.98 GiB (63.25%) | 2,064 | 23.10 | 7.40% | 238.89s | full | FSDP=8 | [daily-field-1074](https://wandb.ai/aurora_gpt/torchtitan.ezpz.train/runs/8hmx63sy) | PASS |
| 20B | 7.66 | 35.07 GiB (88.81%) | 80 | 10.67 | 3.42% | 2205.12s | full | FSDP=8 | [flowing-night-1106](https://wandb.ai/aurora_gpt/torchtitan.ezpz.train/runs/h733f9rs) | PASS |
| 80B | -- | OOM (38.2/39.5 GiB) | -- | -- | -- | -- | full | TP=4, FSDP=2 | [cosmic-mountain-1112](https://wandb.ai/aurora_gpt/torchtitan.ezpz.train/runs/ca4r6921) | OOM |

## Notes

- **20B** runs at 89% memory utilization with heavy CUDA allocation retries.
  Memory-bound at ~10.7 TFLOPS (3.4% MFU). Would benefit from TP=2 or more nodes.
- **80B** does not fit on 8x A100-40GB even with TP=4, FSDP=2, LBS=1, seq=4096.
  PyTorch allocates ~33.4 GiB + 1.4 GiB reserved per GPU, exceeding the 39.5 GiB
  capacity. Needs 4+ nodes or A100-80GB GPUs.

## Known Issues

### torch.compile + `sdpa_kernel(set_priority=True)` on Polaris (FIXED)

Initial runs crashed with `RuntimeError('Invalid backend')` during `torch.compile`
tracing of `F.scaled_dot_product_attention` inside a `sdpa_kernel(..., set_priority=True)`
context manager.

**Root cause:** `torch._dynamo` bug in PyTorch 2.11 — `SDPAKernelVariable.enter()`
in `ctx_manager.py` creates FX graph nodes that pass proxy objects to `int()` when
`set_priority=True`, producing invalid backend IDs for `_set_sdp_priority_order()`.

**Fix:** `EzpzScaledDotProductAttention` subclass overrides `forward()` to use
`sdpa_kernel(self.sdpa_backends)` without `set_priority=True`. Device-aware
`_default_inner_attention()` selects the right subclass (XPU vs CUDA).

### Blendcorpus data cache race condition

When using a `seq_len` or `global_batch_size` that hasn't been cached before,
rank 0 builds the cache files while other ranks race to read them, causing
`FileNotFoundError` or `EOFError`. Workaround: run once to build cache (it will
crash on non-zero ranks), then retry — the cache files persist on disk.
